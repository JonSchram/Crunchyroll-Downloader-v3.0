Namespace processing
    ''' <summary>
    ''' Target that retrieves the raw playlist and writes it to a file so that FFmpeg can be used to download the segments.
    ''' </summary>
    Public Class FileTarget
        Implements ITarget
    End Class
End Namespace